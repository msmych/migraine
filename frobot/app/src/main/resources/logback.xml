<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="POSTGRES" class="ch.qos.logback.classic.db.DBAppender">
        <connectionSource class="ch.qos.logback.core.db.DriverManagerConnectionSource">
            <driverClass>org.postgresql.Driver</driverClass>
            <url>jdbc:postgresql://${FROBOT_DB_HOST}/${FROBOT_DB_NAME}</url>
            <user>${FROBOT_DB_USERNAME}</user>
            <password>${FROBOT_DB_PASSWORD}</password>
        </connectionSource>
        <sqlDialect class="ch.qos.logback.core.db.dialect.PostgreSQLDialect" />
        <insertHeaders>true</insertHeaders>
        <insertAttributes>true</insertAttributes>
        <tableName>logs</tableName>
    </appender>

    <root level="debug">
        <appender-ref ref="STDOUT" />
    </root>
    <root level="INFO">
        <appender-ref ref="POSTGRES"/>
    </root>
</configuration>
